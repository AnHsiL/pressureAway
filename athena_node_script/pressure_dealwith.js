function schedule(before, after ){ 
    
    data.daily_task[i].each_task[j].task.task_detail.length;
};
function dateDiff(Date1, Date2){ 
    var date1 = new Date(Date1);
    var date2 = new Date(Date2);
    var milliseconds_Time = date2.getTime() - date1.getTime();
    return milliseconds_Time / (1000 * 3600 * 24);
};
var data = $(data);
var ori_data = data.ori_data;
var pressureStatus = data.pressureStatus;
for( i=0; i<task[i]; i++ ){
    if (pressureStatus = -1) ;
    else if(pressureStatus = 1) {
        var delay = Math.ceil(data.daily_task[i].length / 2);
        data.daily_task[i] = data.daily_task[i].slice(delay);daily_task[i]/2;
        if(milliseconds_Time <)                                    //差距日小於10天時挪後3天
                                                                   //差距日小於7天時挪後2天

    
        else if(milliseconds_Time)
        var compress_pa = ori_data.dateDiff / data.dateDiff; 
        return compress_pa;
    }

}